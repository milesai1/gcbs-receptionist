~ % mkdir gcbs-receptionist
cd gcbs-receptionist
npm init -y
npm install express twilio dotenv
```

Create a `.env` file:
``` TWILIO_ACCOUNT_SID=AC33a31e31f634f567f1e890964ae49180
TWILIO_AUTH_TOKEN=fa727ebff55a3e3f046f1d78b4165655
TWILIO_PHONE_NUMBER=+61728016134
ADMIN_PHONE_NUMBER=+0755744788
gcbs-receptionist % >....                                 
  res.type('text/xml');
  res.send(twiml.toString());
});

// Placeholder — we'll wire this to the AI in Step 2
app.post('/handle-speech', (req, res) => {
  const customerSaid = req.body.SpeechResult;
  console.log('Customer said:', customerSaid);

  const twiml = new VoiceResponse();
  twiml.say(
    { voice: 'Google.en-AU-Wavenet-C', language: 'en-AU' },
    "Thanks for that! I'm still being set up — but the Australian voice is working perfectly. Stay tuned!"
  );

  res.type('text/xml');
  res.send(twiml.toString());
});

app.listen(process.env.PORT, () => {
  console.log(`Server running on port ${process.env.PORT}`);
});
zsh: parse error near `)'
gcbs-receptionist % [200~git init
zsh: bad pattern: [200~git
gcbs-receptionist % git add .
xcode-select: note: No developer tools were found, requesting install.
If developer tools are located at a non-default location on disk, use `sudo xcode-select --switch path/to/Xcode.app` to specify the Xcode that you wish to use for command line developer tools, and cancel the installation dialog.
See `man xcode-select` for more details.

git remote add origin https://github.com/milesai1/gcbs-receptionist.git
git branch -M main
git push -u origin main
